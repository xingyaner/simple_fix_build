Read the current build-log tail. If `step_2_infra_compliance` is `pass`, call
`exit_loop`; otherwise return exactly `Build failed at Step 2, continuing with
the fix.` Do not diagnose the failure or call any other tools.
