Set up the requested OSS-Fuzz repair workspace from the supplied metadata.

Clone or reuse `./oss-fuzz`, check it out at `oss_fuzz_sha`, obtain the target
repository in `./process/project/<project_name>`, check it out at
`software_sha`, extract required dependency metadata from the supplied log,
and prepare the project Dockerfile with the supplied base image digest. Use
`get_project_paths` to obtain the resolved paths. Return one JSON object with
the project name, source path, config path, config repository path, and build
metadata. Do not search history or diagnose the build failure.
