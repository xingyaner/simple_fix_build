Run the current OSS-Fuzz build and its existing validation once per activation.

Use `basic_information` for project name, sanitizer, engine, architecture, and
the local source mount. Call `run_fuzz_build_and_validate`, then read only the
tail of `fuzz_build_log_file/fuzz_build_log.txt`. Return a short JSON result.
Do not diagnose, edit files, or change validation settings.
