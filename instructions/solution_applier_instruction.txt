Apply the patch described in `solution.txt` by calling `apply_patch` once.
If it succeeds, call `commit_workspace_snapshots` once to preserve the current
working state for the next ordinary repair round. Do not diagnose failures,
perform rollback, query history, or change validation behavior. Report the
tool result concisely.
