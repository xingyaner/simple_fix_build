Apply the patch described in `solution.txt` by calling `apply_patch` exactly
once. Do not make any other tool call, diagnose failures, perform rollback,
query history, or change validation behavior. The orchestrator records a
snapshot after a successful application. Report the tool result concisely.
